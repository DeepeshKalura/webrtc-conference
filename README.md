# WebRTC Conference v1

A real-time video conferencing application built with WebRTC and mediasoup.

## Features

- 🎥 **HD Video Calls** - High-quality video conferencing with multiple participants
- 🎤 **Audio Support** - Crystal clear audio with noise suppression
- 📺 **Screen Sharing** - Share your screen with configurable quality options
- 💬 **Data Channels** - Real-time messaging between participants
- 🔐 **E2E Encryption** - Optional end-to-end encryption for media
- 📱 **Responsive Design** - Works on desktop and mobile browsers

## Configuration via Query Parameters

By adding query parameters into the URL you can set certain settings of the application:

| Parameter          | Type    | Description          | Default Value |
| ------------------ | ------- | -------------------- | ------------- |
| `roomId`           | String  | Id of the room      | Autogenerated  |
| `displayName`      | String  | Display name of your participant | Autogenerated |
| `forceTcp`         | Boolean | Force RTC over TCP instead of UDP | `false` |
| `produce`          | Boolean | Enable sending of audio/video | `true`  |
| `consume`          | Boolean | Enable reception of audio/video | `true` |
| `mic`              | Boolean | Enable microphone by default | `true` |
| `webcam`           | Boolean | Enable webcam by default | `true` |
| `datachannel`      | Boolean | Enable DataChannels | `true` |
| `forceVP8`         | Boolean | Force VP8 codec for webcam and screen sharing | `false` |
| `forceH264`        | Boolean | Force H264 codec for webcam and screen sharing | `false` |
| `forceVP9`         | Boolean | Force VP9 codec for webcam and screen sharing | `false` |
| `forceAV1`         | Boolean | Force AV1 codec for webcam and screen sharing | `false` |
| `screenSharing4K`  | Boolean | Use 4K resolution for screen sharing | `false` |
| `e2eKey`           | String | Key for media E2E encryption/decryption | |

## Installation

### Prerequisites

- Node.js >= 22
- npm or yarn

### Clone and Setup

```sh
git clone https://github.com/DeepeshKalura/webrtc-conference.git
cd webrtc-conference
```

### Server Setup

```sh
cd server
npm install
cp config.example.mjs config.mjs
# Edit config.mjs with your settings
npm start
```

### App Setup

```sh
cd app
npm install --legacy-peer-deps
npm run local
```

## Development

### Running Locally

1. Start the server: `cd server && npm start`
2. Start the app: `cd app && npm run local`
3. Open `https://localhost:3000` in your browser

### Building for Production

```sh
cd app
npm run build
```

## Author

**DeepeshKalura** - [GitHub](https://github.com/DeepeshKalura)

## License

[MIT](./LICENSE)
